# Google Cloud Configuration
GCP_PROJECT_ID=votre-projet-id
GCP_LOCATION=us-central1

# Optionnel: Path vers les crédits (si non configuré via ADC)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/credentials.json
